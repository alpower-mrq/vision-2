import { BingoView } from "@/components/views/BingoView";

/**
 * Bingo vertical — dedicated route. See `/app/casino/page.tsx`
 * for the rationale; same pattern.
 */
export default function BingoPage() {
  return <BingoView />;
}
