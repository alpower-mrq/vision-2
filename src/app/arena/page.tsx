import { ArenaView } from "@/components/views/ArenaView";

/**
 * Arena vertical — dedicated route. See `/app/casino/page.tsx`
 * for the rationale; same pattern.
 */
export default function ArenaPage() {
  return <ArenaView />;
}
