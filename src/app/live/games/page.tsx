import { LiveAllGamesView } from "@/components/views/LiveAllGamesView";

/**
 * Live Casino — "All games" landing. Mirrors /casino/games for the
 * Live vertical: a single full-listing rail of every game in the
 * Live catalogue, reached from the CategoriesSheet's "All games" row.
 */
export default function LiveAllGamesPage() {
  return <LiveAllGamesView />;
}
