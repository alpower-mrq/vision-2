import { LiveView } from "@/components/views/LiveView";

/**
 * Live casino vertical — dedicated route. See `/app/casino/page.tsx`
 * for the rationale; same pattern.
 */
export default function LivePage() {
  return <LiveView />;
}
